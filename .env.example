# Database Configuration
# احصل على هذا الرابط من Neon PostgreSQL (https://neon.tech)
# بعد إنشاء المشروع، انسخ Connection String من Dashboard
DATABASE_URL="postgresql://username:password@host/database?sslmode=require"

# Authentication Secret
# مفتاح سري للـ NextAuth - يجب أن يكون عشوائي وطويل
# يمكنك توليده بالأمر: openssl rand -base64 32
# أو استخدم موقع: https://generate-secret.vercel.app/32
AUTH_SECRET="your-random-secret-key-here-change-this-to-a-secure-random-string"

# Authentication URL
# في التطوير المحلي استخدم localhost
# في Vercel راح يتم ملئه تلقائياً
NEXTAUTH_URL="http://localhost:3000"

# SMTP Email Configuration (for 2FA)
# إعدادات البريد الإلكتروني للتحقق الثنائي
# For Gmail: Use App Password (not your regular password)
# للجيميل: استخدم App Password (ليس كلمة المرور العادية)
# https://support.google.com/accounts/answer/185833
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password-here"
SMTP_FROM_NAME="E-Commerce Store"

# Application URL (for email links)
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# ====================================
# ملاحظات مهمة:
# ====================================
# 1. لا ترفع هذا الملف للـ GitHub! الملف .env محمي تلقائياً
# 2. في Vercel، أضف هذه المتغيرات من Project Settings > Environment Variables
# 3. DATABASE_URL: احصل عليه من Neon بعد إنشاء قاعدة البيانات
# 4. AUTH_SECRET: ولد مفتاح عشوائي جديد للأمان
# 5. NEXTAUTH_URL: في Production ضع رابط موقعك الحقيقي
# 6. SMTP Settings: للجيميل، فعّل التحقق بخطوتين واحصل على App Password
